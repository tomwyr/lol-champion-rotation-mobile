name: lol_champion_rotation
description: "LoL champion rotation checker mobile app."
publish_to: "none"
version: 1.3.0+24

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  app_set_id: ^1.2.1
  cached_network_image: ^3.4.1
  dio: ^5.7.0
  flutter:
    sdk: flutter
  intl: ^0.20.1
  json_annotation: ^4.9.0
  flutter_dotenv: ^5.0.2
  flutter_sticky_header: ^0.7.0
  firebase_core: ^3.9.0
  firebase_messaging: ^15.2.0
  get_it: ^8.0.3
  copy_with_extension: ^6.0.0
  toastification: ^2.3.0
  shared_preferences: ^2.3.5
  package_info_plus: ^8.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  json_serializable: ^6.8.0
  build_runner: ^2.4.11
  copy_with_extension_gen: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/images/
